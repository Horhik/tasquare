import React from "react";
import { connect } from "react-redux";

const ChangeDurationPopUp = props => {
  return (
    <section>
      <button> close</button>
      <div></div>
    </section>
  );
};
export default connect()(ChangeDurationPopUp);
